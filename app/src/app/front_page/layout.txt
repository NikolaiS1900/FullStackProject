import { Metadata } from 'next';

export const metadata: Metadata = {
    title: {
        template: '%s | Craft Gallery',
        default: 'Front Page'
    },
    description: 'This is a craft gallery for my girl friend',
    metadataBase: new URL('https://full-stack-project-cy9rgvi8w-nikolais1900s-projects.vercel.app/')
};


export default function Layout({ children }: { children: React.ReactNode }) {
    return (
        <div className="flex h-screen flex-col md:flex-row md:overflow-hidden">
            <div className="w-full flex-node md:w-64">
                <SideNav />
            </div>
        </div>
    )
}